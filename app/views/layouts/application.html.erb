<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>SimplecodecastsSaas</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <%= link_to root_path, class: 'navbar-brand' do %>
        <div class="navbar__brand-container">
          <i class="fa fa-users navbar__brand-icon"></i>
          <span class="navbar__brand-title">SaaS</span>
        </div>
      <% end %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-group">
          <% if current_user %>
            <div class="navbar-nav">
              <%= link_to 'Logout', destroy_user_session_path, class: 'btn btn-secondary navbar-btn navbar-btn--logout', method: :delete %>
            </div>
          <% else %>
            <div class="navbar-nav">
              <%= link_to 'Login', new_user_session_path, class: 'btn btn-primary navbar-btn navbar-btn--login', method: :get %>
            </div>
          <% end %>
        </div>
        <div class="navbar-nav">
          <%= link_to 'About', about_path, class: 'nav-item nav-link' %>
        </div>
        <div class="navbar-nav">
          <%= link_to 'Contact', new_contact_path, class: 'nav-item nav-link' %>
        </div>
      </div>
    </nav>
    <div class="container-fluid">
      <%= yield %>
    </div>
  </body>
</html>
